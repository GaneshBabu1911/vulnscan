# ── Render Blueprint Configuration for VulnScan ─────────────────────────────
services:
  # ── Backend Web Service (Flask API + Nmap scanner in Docker) ───────────────
  - type: web
    name: vulnscan-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    region: oregon
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: ADMIN_PASSWORD
        value: Admin@123456
      - key: AI_PROVIDER
        value: local
      # Set DATABASE_URL in Render Dashboard if using external cloud MySQL
      - key: FRONTEND_URL
        fromService:
          type: web
          name: vulnscan-frontend
          property: host
      - key: CORS_ORIGINS
        fromService:
          type: web
          name: vulnscan-frontend
          property: host

  # ── Frontend Static Site (React Vite) ──────────────────────────────────────
  - type: web
    name: vulnscan-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: vulnscan-backend
          property: host
